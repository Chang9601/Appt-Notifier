<!DOCTYPE html>
<html lang="en">
<head>
<title>예약삭제</title>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>

<style>
	th, td {
		text-align: center;	
	}
</style>

</head>
<body>
	<div class="container mt-3">
  		<input type="hidden" class="form-control" id="appts" value="{{appts}}">		
		<table class="table table-hover">
			<thead>
				<tr>
					<th>날짜</th>
					<th>시간</th>
					<th>이름</th>
					<th>전화번호</th>
					<th>취소</th>
				</tr>
			</thead>
			<tbody>
				{{#appts}}
				<tr>
					<td id="appt-date">{{apptDate}}</td>
					<td>{{apptTime}}</td>
					<td>{{clientName}}</td>
					<td>{{clientPhone}}</td>
					<td><button>🪓</button></td>
				</tr>
				{{/appts}}
			</tbody>
		</table>
	</div>

	<ul class="pagination justify-content-center">
	    {{#is-prev}}
	    	<li class="page-item"><a class="page-link" href="?page={{prev}}">이전</a></li>
	    {{/is-prev}}
	    {{^is-prev}}
	    	<li class="page-item disabled"><a class="page-link" href="?page={{prev}}">이전</a></li>
	    {{/is-prev}}
	    
	    {{#is-next}}
	        <li class="page-item"><a class="page-link" href="?page={{next}}">다음</a></li>
	    {{/is-next}}
	    {{^is-next}}
	        <li class="page-item disabled"><a class="page-link" href="?page={{next}}">다음</a></li>
	    {{/is-next}}
	</ul>	
</body>

<script>
	document.querySelector('button').addEventListener('click', () => {
		let val = confirm('해당 예약을 취소하시겠습니까?');
		
		let apptDate = ((document.getElementById('appt-date')).innerHTML).trim();
		
		console.log(apptDate);
	
		/*
		let clientName = document.querySelector('#name').value;
		let clientPhone = document.querySelector('#phone').value;
		//let apptDate = document.querySelector('#').value;
		//let apptTime = document.querySelector('#phone').value;
				
		let data = {
			clientName: clientName,
			clientPhone: clientPhone,
		};
		
		if(val === false) {
			return;
		}
		
		$.ajax({
			type: 'DELETE',
			url: '/api/appt/delete',
			data: JSON.stringify(data),
			contentType: 'application/json; charset=UTF-8', // 서버에 요청하는 자료형
			dataType: 'json' // 서버가 응답하는 자료형
		}).done(function(resp) {
			if (resp.data === null) {
				alert('입력하신 정보와 일치하는 예약이 없습니다.');
				return;
			}
			console.log(resp);
			
		}).fail(function(error) {
			alert(JSON.stringify(error));
		});		
		*/
	});
</script>
</html>